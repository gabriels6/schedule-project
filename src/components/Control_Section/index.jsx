import React from 'react';
import {Main} from './styles';

const Control_Section = ({children}) => {

    return(
        <Main>
            {children}
        </Main>
    )
}

export default Control_Section;